Se creo un controller para las recetas controller/recetaController.js
Se agregó un endpoint para analizar las recetas routes/recetaRoutes.js

en app.js se agregó:
app.use(express.json({ limit: '10mb' })); 
app.use(express.urlencoded({ limit: '10mb', extended: true }));